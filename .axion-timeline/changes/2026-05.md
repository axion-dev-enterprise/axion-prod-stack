# 2026-05

## 2026-05-10 - FLOW PicoClaw LLM auth/global tenant repair

- Live VPS `203.161.39.174`:
  - Updated `tenant.env` for `axion-official`, `lucas-mateus`, `jhonathan-italinea`, `felipe-adonio`, and `victor-siebra` to include the global LLM credential variables from `/opt/axion/axion-prod-stack/env/.env`.
  - Backed up each tenant config/env before mutation.
  - Rewrote each tenant `data/config.json` model config to the current PicoClaw-compatible shape:
    - `agents.defaults.model_name = "gpt-4o"`
    - single model entry for `gpt-4o`
    - OpenAI base URL
    - both `api_key` and `api_keys`
    - native web search disabled to avoid `web_search_preview` chat-completions errors.
  - Restarted all five tenant containers.
- Repository:
  - Updated `scripts/tenants/render_picoclaw_config.py` so future renders support `FLOW_LLM_PROVIDER=openai`, emit `api_keys`, and default native web search off.
- Verification:
  - `python -m py_compile scripts/tenants/render_picoclaw_config.py`
  - Remote `python3 -m py_compile /opt/axion/axion-prod-stack/scripts/tenants/render_picoclaw_config.py`
  - Public health checks returned `200` for `flow.axionenterprise.cloud`, `lucas-mateus`, `jhonathan-italinea`, `felipe-adonio`, and `victor-siebra`.
  - Post-fix logs showed `turn_end status=completed` for all five PicoClaw tenants.
