# 2026-05

## 2026-05-10 - FLOW global LLM 401 incident

- Located FLOW runtime on VPS `203.161.39.174` using workspace memory, project docs, and VAULT references.
- Confirmed active PicoClaw tenants: `axion-official`, `lucas-mateus`, `jhonathan-italinea`, `felipe-adonio`, `victor-siebra`.
- Verified global OpenAI and OpenRouter credentials on-host without printing secrets.
- Found root cause chain:
  - Most tenant containers had no LLM keys in `tenant.env`.
  - After env propagation, PicoClaw still sent unauthenticated OpenAI calls because the current binary expected a plain `gpt-4o` model entry with `api_keys`, not the older `openai/gpt-4o` multi-entry shape.
  - Native web search tool injection produced a secondary `400 web_search_preview` error against chat completions.
- Patched live tenant configs to use `gpt-4o`, include `api_key` plus `api_keys`, disable native web search, and restarted all five tenant containers.
- Validation: all five tenants logged `turn_end status=completed` after restart with no new 401/400 in the post-fix check.
